class Solution {
public:
    int fib(int n) {
        //if(n>=1){
          if(n<=1){
            //f(n)=f(n-1)+f(n-2);
            return n;
        }
       // return fib(n)=fib(n-1)+fib(n-2);
          return fib(n-1)+fib(n-2);
    }
};
